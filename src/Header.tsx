import React from "react";

export const Header = () => {
    return (
        <div className="header-container">
            <h1>Todo list</h1>
            <button className="theme-button">Theme</button>
        </div>
    );
}